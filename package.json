{
  "name": "aws-smtp-relay",
  "version": "0.1.0",
  "bin": {
    "aws-smtp-relay": "bin/aws-smtp-relay.js"
  },
  "scripts": {
    "buildLambda": "cd lambda && npm install && npm run build && cd ..",
    "build": "tsc && npm run buildLambda",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "22.7.9",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.5",
    "aws-cdk": "2.1031.1",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.221.1",
    "constructs": "^10.4.2"
  }
}
